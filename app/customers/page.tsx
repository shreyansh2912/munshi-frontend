/**
 * Customers Page (Server Component Entry Point)
 * Delegates to Container for data fetching
 */

import CustomersContainer from './CustomersContainer';

export default function CustomersPage() {
  return <CustomersContainer />;
}
