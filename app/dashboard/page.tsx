/**
 * Dashboard Page (Server Component Entry Point)
 * Delegates to Container for data fetching
 */

import DashboardContainer from './DashboardContainer';

export default function DashboardPage() {
  return <DashboardContainer />;
}
